-- SELECT first_name, last_name
-- FROM customers;

-- SELECT
-- 	last_name,
--     first_name,
--     points,
--     -- points + 10
--     (points + 10) * 100 AS 'Discount Factor'
-- FROM customers

SELECT DISTINCT state
FROM customers